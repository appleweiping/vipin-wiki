# raw

This directory stores source materials.

Rules:

- Files here are the source of truth.
- Agents may read from `raw/`, but should not modify source contents during normal wiki maintenance.
- Put newly collected items into `raw/inbox/` unless you already have a better organizing scheme.

